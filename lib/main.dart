import 'package:flutter/material.dart';
import 'package:atma9/PAGEPILIHAN.dart';

void main() => runApp(MyApp());

class MyApp extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: PagePilihan()
    );
  }
}
